# Student Guide Bot

A browser-based study-planning helper. It stores study tasks in the browser and provides rule-based chat replies for daily study planning.

## Use

Open `index.html` in a browser, or publish this repository with GitHub Pages.

## Current limits

This first version does not connect to an AI provider, calendar, or user account. Tasks remain only in the browser where they are created.
